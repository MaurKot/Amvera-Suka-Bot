meta:
  environment: docker
  toolchain:
    name: docker
build:
  type: docker
  dockerfile: Dockerfile
run:
  containerPort: 8080
  persistenceMount: /data
env:
  NODE_ENV: production
  PORT: "8080"
